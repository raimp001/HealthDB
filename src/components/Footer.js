import React from 'react';
import { Link } from 'react-router-dom';

const Footer = () => {
  // Footer is now included in LandingPage for design consistency
  // This component is kept for other pages that might need a standalone footer
  return null;
};

export default Footer;
